(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[931],{4400:function(e,n,t){Promise.resolve().then(t.bind(t,9275))},9275:function(e,n,t){"use strict";t.d(n,{default:function(){return f}});var a=t(7437),r=t(2265),o=t(9376),i=t(7648);let s=Math.PI/180;function l(e){let n=e.trim().replace("#",""),t=parseInt(3===n.length?n[0]+n[0]+n[1]+n[1]+n[2]+n[2]:n.slice(0,6),16);return[(t>>16&255)/255,(t>>8&255)/255,(255&t)/255].map(e=>e<=.04045?e/12.92:Math.pow((e+.055)/1.055,2.4))}function u(e){let{distance:n=24,elevation:t=-5.5,azimuth:o=0,orbitSpeed:i=0,roll:u=-20,fov:f=42,diskInner:h=3,diskOuter:c=15,diskThickness:d=.26,diskDensity:m=1,brightness:p=1,spinSpeed:v=.06,grain:g=.48,doppler:x=.35,hotColor:b="#F5EFEB",midColor:w="#E58C36",coolColor:y="#B45309",starBrightness:E=0,glow:T=1,exposure:k=.9,vignette:D=.28,steps:S=300,resolution:_=.7,maxDpr:M=1.75,focus:A=[.72,.46],scrim:R="none",scrimStrength:F=.9,paused:C=!1,className:U="",children:P,...L}=e,B=(0,r.useRef)(null),I=(0,r.useRef)(null),N=(0,r.useRef)({distance:n,elevation:t,azimuth:o,orbitSpeed:i,roll:u,fov:f,diskInner:h,diskOuter:c,diskThickness:d,diskDensity:m,brightness:p,spinSpeed:v,grain:g,doppler:x,hotColor:b,midColor:w,coolColor:y,starBrightness:E,glow:T,exposure:k,vignette:D,steps:S,resolution:_,maxDpr:M,focus:A,scrim:R,scrimStrength:F,paused:C});return N.current={distance:n,elevation:t,azimuth:o,orbitSpeed:i,roll:u,fov:f,diskInner:h,diskOuter:c,diskThickness:d,diskDensity:m,brightness:p,spinSpeed:v,grain:g,doppler:x,hotColor:b,midColor:w,coolColor:y,starBrightness:E,glow:T,exposure:k,vignette:D,steps:S,resolution:_,maxDpr:M,focus:A,scrim:R,scrimStrength:F,paused:C},(0,r.useEffect)(()=>{let e=B.current,n=I.current;if(!e||!n)return;let t="function"==typeof window.matchMedia&&window.matchMedia("(prefers-reduced-motion: reduce)").matches,a={alpha:!1,antialias:!1,depth:!1,stencil:!1,powerPreference:"high-performance",preserveDrawingBuffer:!1},r=n.getContext("webgl2",a)||n.getContext("webgl",a);function o(t){e.dataset.webgl=t,n.style.display="none"}if(!r){o("unsupported");return}let i=r.getExtension("WEBGL_debug_renderer_info"),u=i?String(r.getParameter(i.UNMASKED_RENDERER_WEBGL)||""):"",f=/swiftshader|llvmpipe|softpipe|software|microsoft basic/i.test(u),h="undefined"!=typeof WebGL2RenderingContext&&r instanceof WebGL2RenderingContext;function c(e,n){let t=r.createShader(e);return t?(r.shaderSource(t,n),r.compileShader(t),r.getShaderParameter(t,r.COMPILE_STATUS))?t:(console.error("blackhole: shader failed —",r.getShaderInfoLog(t)||"no log (context lost?)"),r.deleteShader(t),null):null}function d(e){let n=c(r.VERTEX_SHADER,"\nattribute vec2 aPos;\nvarying vec2 vUv;\nvoid main() {\n  vUv = aPos * 0.5 + 0.5;\n  gl_Position = vec4(aPos, 0.0, 1.0);\n}\n"),t=c(r.FRAGMENT_SHADER,e);if(!n||!t)return null;let a=r.createProgram();if(!a)return null;if(r.attachShader(a,n),r.attachShader(a,t),r.bindAttribLocation(a,0,"aPos"),r.linkProgram(a),r.deleteShader(n),r.deleteShader(t),!r.getProgramParameter(a,r.LINK_STATUS))return console.error(r.getProgramInfoLog(a)),null;let o={},i=r.getProgramParameter(a,r.ACTIVE_UNIFORMS);for(let e=0;e<i;e++){let n=r.getActiveUniform(a,e);n&&(o[n.name]=r.getUniformLocation(a,n.name))}return{program:a,u:o}}let m=!0,p=r.UNSIGNED_BYTE,v=r.RGBA;if(h)r.getExtension("EXT_color_buffer_half_float")||r.getExtension("EXT_color_buffer_float")?(p=r.HALF_FLOAT,v=r.RGBA16F):m=!1;else{let e=r.getExtension("OES_texture_half_float"),n=r.getExtension("EXT_color_buffer_half_float");e&&n?p=e.HALF_FLOAT_OES:m=!1}m||(p=r.UNSIGNED_BYTE,v=r.RGBA);let g=h||r.getExtension("OES_texture_half_float_linear")||!m?r.LINEAR:r.NEAREST,x=m?1:.12;function b(e,n){let t=r.createTexture(),a=r.createFramebuffer();if(!t||!a)return null;r.bindTexture(r.TEXTURE_2D,t),r.texImage2D(r.TEXTURE_2D,0,v,e,n,0,r.RGBA,p,null),r.texParameteri(r.TEXTURE_2D,r.TEXTURE_MIN_FILTER,g),r.texParameteri(r.TEXTURE_2D,r.TEXTURE_MAG_FILTER,g),r.texParameteri(r.TEXTURE_2D,r.TEXTURE_WRAP_S,r.CLAMP_TO_EDGE),r.texParameteri(r.TEXTURE_2D,r.TEXTURE_WRAP_T,r.CLAMP_TO_EDGE),r.bindFramebuffer(r.FRAMEBUFFER,a),r.framebufferTexture2D(r.FRAMEBUFFER,r.COLOR_ATTACHMENT0,r.TEXTURE_2D,t,0);let o=r.checkFramebufferStatus(r.FRAMEBUFFER);return(r.bindFramebuffer(r.FRAMEBUFFER,null),o!==r.FRAMEBUFFER_COMPLETE)?(r.deleteTexture(t),r.deleteFramebuffer(a),null):{fb:a,tex:t,w:e,h:n}}let w=null,y=null,E=null,T=null,k=null,D=null,S=null,_=null,M=null,A=null,R=null,F=0,C=0,U=0,P=0,L=0;function O(){return w=d("\nprecision highp float;\n\n#define MAX_STEPS 460\n\n/**\n * Seconds before the wound-up gas pattern hands over to a fresh copy. What\n * matters is how far the gas winds in one of these, which is this times the\n * spin — so a slow disc can afford a long cycle, and a long cycle is what you\n * want: every handover costs a little contrast while the two copies overlap.\n */\n#define WIND_CYCLE 46.0\n\nvarying vec2 vUv;\n\nuniform vec2  uRes;\nuniform float uTime;\nuniform vec3  uCamPos;\nuniform vec3  uRight;\nuniform vec3  uUp;\nuniform vec3  uFwd;\nuniform float uTanHalf;\nuniform vec2  uFocus;\nuniform float uSteps;\nuniform float uSkyR;\nuniform float uDiskIn;\nuniform float uDiskOut;\nuniform float uThick;\nuniform float uDensity;\nuniform float uSpin;\nuniform float uGrain;\nuniform float uBright;\nuniform float uDoppler;\nuniform vec3  uHot;\nuniform vec3  uMid;\nuniform vec3  uCool;\nuniform float uStars;\nuniform float uEncode;\nuniform vec2  uJitter;\nuniform float uSeed;\n\n/* --- noise ---------------------------------------------------------------- */\n\nfloat hash13(vec3 p) {\n  p = fract(p * 0.3183099 + vec3(0.1, 0.2, 0.3));\n  p *= 17.0;\n  return fract(p.x * p.y * p.z * (p.x + p.y + p.z));\n}\n\nfloat vnoise(vec3 x) {\n  vec3 i = floor(x);\n  vec3 f = fract(x);\n  f = f * f * (3.0 - 2.0 * f);\n  float n000 = hash13(i + vec3(0.0, 0.0, 0.0));\n  float n100 = hash13(i + vec3(1.0, 0.0, 0.0));\n  float n010 = hash13(i + vec3(0.0, 1.0, 0.0));\n  float n110 = hash13(i + vec3(1.0, 1.0, 0.0));\n  float n001 = hash13(i + vec3(0.0, 0.0, 1.0));\n  float n101 = hash13(i + vec3(1.0, 0.0, 1.0));\n  float n011 = hash13(i + vec3(0.0, 1.0, 1.0));\n  float n111 = hash13(i + vec3(1.0, 1.0, 1.0));\n  return mix(\n    mix(mix(n000, n100, f.x), mix(n010, n110, f.x), f.y),\n    mix(mix(n001, n101, f.x), mix(n011, n111, f.x), f.y),\n    f.z\n  );\n}\n\n/** lod fades the finest octave out, for rays whose steps are too long to see it. */\nfloat fbm(vec3 p, float lod) {\n  float a = 0.5;\n  float s = 0.0;\n  for (int i = 0; i < 4; i++) {\n    s += (i == 3 ? a * lod : a) * vnoise(p);\n    p = p * 2.03 + vec3(11.3, 7.1, 3.7);\n    a *= 0.5;\n  }\n  return s;\n}\n\n/* --- the gas -------------------------------------------------------------- */\n\n/**\n * Density and colour of the disc at a point.\n *\n * The gas runs on Kepler orbits, so the inner rim laps the outer edge many\n * times over. Reading the turbulence in a frame that turns with the gas, at\n * each radius its own rate, is what shears the clouds into the trailing\n * spirals — nothing draws a spiral.\n */\nvoid gasAt(vec3 p, float rd, float dt, out float dens, out vec3 tint, out float heat) {\n  float rn = clamp((rd - uDiskIn) / max(0.001, uDiskOut - uDiskIn), 0.0, 1.0);\n\n  // A thin sheet at the rim, flaring outward.\n  float tk = uThick * (0.35 + 1.25 * rn);\n  float v = p.y / tk;\n  float sheet = exp(-v * v);\n\n  // Detail the ray cannot resolve is detail it should not be asking for. A ray\n  // running the length of the disc steps about a tenth of a unit at a time,\n  // and the finest octave is finer than that — sampled once each, those cells\n  // do not average out, they beat, and the arms come out combed with dashes.\n  // So the last octave fades out as the step grows.\n  float lod = clamp(1.0 - dt * uGrain * 14.0, 0.0, 1.0);\n\n  float phi = atan(p.z, p.x);\n  // Kepler: omega goes as r^-3/2, so every radius turns at its own rate and\n  // the clouds are read in a frame that turns with them. That shear is what\n  // draws the trailing spirals — nothing here draws a spiral.\n  float omega = uSpin * pow(uDiskIn / rd, 1.5);\n  // A third axis, so two radii turning at their own rates do not read the same\n  // cloud, plus a slow creep inward so the gas falls as well as turns.\n  float lr = log(rd) * 1.1 + uSpin * uTime * 0.05;\n\n  // Left alone, that shear never stops winding: the pattern at one radius\n  // slides past its neighbour for as long as the page is open, so the spiral\n  // tightens without limit and within a minute it is finer than a pixel and\n  // tears into moire. Real gas is spared this because turbulence keeps\n  // rebuilding it. Here two copies of the disc run the same wind on clocks\n  // half a cycle apart, and the picture crossfades from one to the other, each\n  // handing over while the other is still young. Nothing ever winds past one\n  // cycle's worth, and the crossfade lands where its layer is weightless.\n  float u = uTime / WIND_CYCLE;\n  float fA = fract(u);\n  float fB = fract(u + 0.5);\n  float w = abs(2.0 * fA - 1.0);\n\n  float cloudsA = fbm(vec3(vec2(cos(phi + omega * fA * WIND_CYCLE),\n                                sin(phi + omega * fA * WIND_CYCLE)) * (rd * uGrain), lr), lod);\n  float cloudsB = fbm(vec3(vec2(cos(phi + omega * fB * WIND_CYCLE),\n                                sin(phi + omega * fB * WIND_CYCLE)) * (rd * uGrain), lr + 40.0), lod);\n  float clouds = mix(cloudsA, cloudsB, w);\n\n  // Squared for the density only, so the gaps between the filaments go\n  // properly dark instead of filling in as haze. The temperature below keeps\n  // reading the smooth version: heat should not have hard edges.\n  float filaments = clouds * clouds * 1.75;\n\n  // Bright at the rim, gone by the outer edge, gone again just inside it.\n  float inner = smoothstep(0.0, 0.07, rn);\n  float outer = 1.0 - smoothstep(0.45, 1.0, rn);\n  float prof = inner * outer * pow(uDiskIn / rd, 2.0);\n\n  dens = max(0.0, filaments * 1.5 - 0.30) * sheet * prof * uDensity * 4.6;\n\n  // Shakura–Sunyaev: T falls as r^-3/4. The colour ramp rides it.\n  heat = pow(uDiskIn / rd, 0.8) * (0.72 + 0.55 * clouds);\n  tint = mix(uCool, uMid, smoothstep(0.10, 0.52, heat));\n  tint = mix(tint, uHot, smoothstep(0.52, 1.05, heat));\n}\n\n/* --- stars ---------------------------------------------------------------- */\n\n/**\n * Stars are laid out on the six faces of a cube and read through whichever\n * face the ray leaves by. A grid in space would work too, until you notice\n * that a cube of space cut by the sphere of directions is a long thin sliver,\n * and every star comes out as a scratch.\n *\n * They are drawn small on purpose. The ray arrives here already bent, so this\n * sky is a lensed sky, and lensing stretches whatever it magnifies sideways.\n * A real star has no width to stretch and stays a point that merely brightens;\n * a fat blob drawn here would smear into a long arc halfway across the frame.\n * Keeping the blob near a pixel wide holds the smear to the ring around the\n * shadow, which is the one place it belongs.\n */\nvec3 starField(vec3 d) {\n  vec3 a = abs(d);\n  vec2 uv;\n  float face;\n  if (a.x >= a.y && a.x >= a.z)      { uv = d.yz / a.x; face = d.x > 0.0 ? 0.0 : 1.0; }\n  else if (a.y >= a.z)               { uv = d.xz / a.y; face = d.y > 0.0 ? 2.0 : 3.0; }\n  else                               { uv = d.xy / a.z; face = d.z > 0.0 ? 4.0 : 5.0; }\n\n  vec3 col = vec3(0.0);\n  for (int k = 0; k < 3; k++) {\n    float sc = 90.0 * pow(2.2, float(k));\n    vec2 p = uv * sc;\n    vec2 id = floor(p);\n    vec2 f = fract(p) - 0.5;\n    float h = hash13(vec3(id, face * 19.0));\n    if (h > 0.965) {\n      vec2 off = vec2(hash13(vec3(id, face + 11.0)), hash13(vec3(id, face + 23.0)));\n      float dd = length(f - (off - 0.5) * 0.7);\n      float s = smoothstep(0.055, 0.0, dd);\n      float warm = hash13(vec3(id, face + 51.0));\n      col += s * (0.6 + 4.5 * fract(h * 97.0))\n           * mix(vec3(0.72, 0.82, 1.0), vec3(1.0, 0.88, 0.72), warm)\n           / pow(2.2, float(k));\n    }\n  }\n  // A breath of dust, so the sky is not flat black between the points.\n  col += vec3(0.013, 0.017, 0.030) * fbm(d * 2.6, 1.0);\n  return col;\n}\n\n/* --- march ---------------------------------------------------------------- */\n\nvoid main() {\n  // The ray leaves from somewhere else inside its pixel every frame, and the\n  // frames are averaged. One ray per pixel cannot resolve what happens at the\n  // shadow's rim: rays that skim the photon sphere land on wildly different\n  // parts of the disc for a hair's difference in aim, so a single sample there\n  // is a coin toss and the rim breaks into loose pixels. Ten-odd tosses per\n  // pixel, gathered over time, is what settles it.\n  vec2 uv = (gl_FragCoord.xy + uJitter - uFocus * uRes) / uRes.y;\n  vec3 dir = normalize(uFwd + (uv.x * uRight + uv.y * uUp) * 2.0 * uTanHalf);\n\n  vec3 pos = uCamPos;\n  vec3 vel = dir;\n\n  // Angular momentum. Conserved, so it is read once and carried.\n  vec3 hv = cross(pos, vel);\n  float h2 = dot(hv, hv);\n  float h = sqrt(h2);\n  /** Angle swept around the hole so far. dphi/ds = h/r\xb2, and h is constant. */\n  float swept = 0.0;\n\n  vec3 col = vec3(0.0);\n  float transmit = 1.0;\n  bool captured = false;\n\n  // Where inside its step each ray reads the gas. Always reading the middle\n  // beats the step pattern against the disc into rings — worst in the halo,\n  // where lensing packs a hundred radii into a few pixels. So each pixel\n  // starts somewhere else, and then walks the read on by the golden ratio at\n  // every step: one fixed offset per pixel is not enough, because the steps\n  // themselves shorten in a smooth pattern as the ray nears the disc and a\n  // fixed offset rides that pattern instead of breaking it up.\n  float jitter = fract(sin(dot(gl_FragCoord.xy + uSeed, vec2(12.9898, 78.233))) * 43758.5453);\n\n  for (int i = 0; i < MAX_STEPS; i++) {\n    if (float(i) >= uSteps) break;\n\n    float r2 = dot(pos, pos);\n    float r = sqrt(r2);\n\n    if (r < 1.0) { captured = true; break; }          // through the horizon\n    if (r > uSkyR && dot(pos, vel) > 0.0) break;      // gone, and not coming back\n    if (transmit < 0.004) break;                      // nothing behind this is visible\n\n    float dt = clamp(0.14 * (r - 1.0), 0.025, 1.1);\n\n    // Never step more than half the way to the disc plane while the disc is\n    // still in radial reach, or a grazing ray tunnels clean through it.\n    if (r < uDiskOut * 1.25) {\n      float rn = clamp((r - uDiskIn) / max(0.001, uDiskOut - uDiskIn), 0.0, 1.0);\n      float tk = uThick * (0.35 + 1.25 * rn);\n      dt = min(dt, max(tk * 0.38, abs(pos.y) * 0.5));\n    }\n\n    swept += h * dt / r2;\n\n    // How much the deeper images are worth. A ray that skims the photon sphere\n    // wraps the hole again and again, and each wrap paints another, tighter\n    // copy of the disc against the shadow. Those copies are real, and they are\n    // faint: every extra turn costs a factor of about e^2pi. Given full\n    // brightness they instead pile into a hairline ring which no single ray\n    // per pixel can resolve — past the photon sphere the arrival angle swings\n    // wildly between neighbouring pixels, so the ring comes out as a broken\n    // string of sparks that crawl as the gas turns. Charging each turn its\n    // proper cost puts it back in its place: a soft rim, not a dotted circle.\n    // Half a turn is free, which leaves the halo — that one only bends.\n    float deep = exp(-1.3 * max(0.0, swept - 4.6));\n\n    jitter = fract(jitter + 0.6180339887);\n    vec3 mid = pos + vel * (dt * jitter);\n    float rd = length(mid.xz);\n\n    if (rd > uDiskIn && rd < uDiskOut && abs(mid.y) < uThick * 5.0) {\n      float dens;\n      float heat;\n      vec3 tint;\n      gasAt(mid, rd, dt, dens, tint, heat);\n\n      if (dens > 0.001) {\n        // Beaming. The gas orbits at sqrt(M/r) with M = 1/2, so 0.41c at the\n        // rim. g folds in the boost and the climb out of the well; flux goes\n        // as g\xb3, and uDoppler dials that exponent down to nothing.\n        vec3 tang = normalize(cross(vec3(0.0, 1.0, 0.0), vec3(mid.x, 0.0, mid.z)));\n        float beta = min(0.85, sqrt(0.5 / max(rd, 1.5)));\n        float gam = inversesqrt(max(1e-4, 1.0 - beta * beta));\n        vec3 toObs = -normalize(vel);\n        float g = 1.0 / (gam * (1.0 - beta * dot(tang, toObs)));\n        g *= sqrt(max(0.05, 1.0 - 1.0 / rd));\n        float boost = pow(max(g, 0.02), 3.0 * uDoppler);\n\n        // Coming at you it also runs blue, going away it runs red.\n        vec3 shift = mix(\n          vec3(1.0),\n          g > 1.0 ? vec3(0.86, 0.94, 1.14) : vec3(1.15, 0.82, 0.62),\n          clamp(abs(g - 1.0) * 1.6, 0.0, 1.0) * uDoppler\n        );\n\n        float emit = uBright * (0.26 + 2.0 * heat * heat);\n        col += tint * shift * (emit * boost * dens * transmit * dt * deep);\n        transmit *= exp(-dens * 0.30 * dt);\n      }\n    }\n\n    // u'' + u = 3M u\xb2, in Cartesian form.\n    vec3 acc = -1.5 * h2 * pos / (r2 * r2 * r);\n    vel += acc * dt;\n    pos += vel * dt;\n  }\n\n  if (!captured && uStars > 0.001) {\n    // Lensing stretches an image sideways, and a star's flux does not grow to\n    // fill it: a magnified point stays as bright, it does not become a bright\n    // streak. The stretch is the ratio of the angle the ray left at to the\n    // angle it ended up travelling at, both measured off the line to the hole,\n    // and dividing the light by it puts the flux back where it belongs. Long\n    // arcs then fade as they lengthen instead of scratching across the frame.\n    vec3 toHole = normalize(-uCamPos);\n    float sI = length(cross(normalize(dir), toHole));\n    float sS = length(cross(normalize(vel), toHole));\n    float stretch = clamp(sI / max(1e-3, sS), 1.0, 40.0);\n    col += starField(normalize(vel)) * uStars * transmit / stretch;\n  }\n\n  if (uEncode > 0.5) col = col / (1.0 + col);\n  gl_FragColor = vec4(col, 1.0);\n}\n"),y=d("\nprecision highp float;\nvarying vec2 vUv;\nuniform sampler2D uCur;\nuniform sampler2D uPrev;\nuniform float uAlpha;\n\nvoid main() {\n  vec3 c = texture2D(uCur, vUv).rgb;\n  vec3 p = texture2D(uPrev, vUv).rgb;\n  gl_FragColor = vec4(mix(p, c, uAlpha), 1.0);\n}\n"),E=d("\nprecision highp float;\nvarying vec2 vUv;\nuniform sampler2D uTex;\nuniform vec2 uTexel;\nuniform float uDecode;\nuniform float uPack;\nuniform float uThreshold;\n\nvoid main() {\n  vec3 s = texture2D(uTex, vUv + uTexel * vec2(-1.0, -1.0)).rgb\n         + texture2D(uTex, vUv + uTexel * vec2( 1.0, -1.0)).rgb\n         + texture2D(uTex, vUv + uTexel * vec2(-1.0,  1.0)).rgb\n         + texture2D(uTex, vUv + uTexel * vec2( 1.0,  1.0)).rgb;\n  s *= 0.25;\n  if (uDecode > 0.5) s = s / max(vec3(0.002), 1.0 - s);\n  float l = max(s.r, max(s.g, s.b));\n  s *= max(0.0, l - uThreshold) / max(0.0001, l);\n  gl_FragColor = vec4(s * uPack, 1.0);\n}\n"),T=d("\nprecision highp float;\nvarying vec2 vUv;\nuniform sampler2D uTex;\nuniform vec2 uStep;\n\nvoid main() {\n  vec3 s = texture2D(uTex, vUv).rgb * 0.2270270;\n  s += (texture2D(uTex, vUv + uStep * 1.3846154).rgb\n      + texture2D(uTex, vUv - uStep * 1.3846154).rgb) * 0.3162162;\n  s += (texture2D(uTex, vUv + uStep * 3.2307692).rgb\n      + texture2D(uTex, vUv - uStep * 3.2307692).rgb) * 0.0702702;\n  gl_FragColor = vec4(s, 1.0);\n}\n"),k=d("\nprecision highp float;\nvarying vec2 vUv;\nuniform sampler2D uScene;\nuniform sampler2D uBloom;\nuniform vec2  uRes;\nuniform float uDecode;\nuniform float uPack;\nuniform float uGlow;\nuniform float uExposure;\nuniform float uVignette;\nuniform float uScrimDir;\nuniform float uScrimAmt;\nuniform float uSeed;\n\nvec3 aces(vec3 x) {\n  return clamp((x * (2.51 * x + 0.03)) / (x * (2.43 * x + 0.59) + 0.14), 0.0, 1.0);\n}\n\nvoid main() {\n  vec3 scene = texture2D(uScene, vUv).rgb;\n  if (uDecode > 0.5) scene = scene / max(vec3(0.002), 1.0 - scene);\n  vec3 bloom = texture2D(uBloom, vUv).rgb / uPack;\n\n  vec3 c = scene + bloom * uGlow;\n  c = aces(c * uExposure);\n  c = pow(max(c, 0.0), vec3(0.4545));\n\n  // Corners down.\n  vec2 d = vUv - 0.5;\n  c *= 1.0 - uVignette * dot(d, d) * 1.9;\n\n  // The edge the copy sits on: heavy at the edge, off quickly, so the middle\n  // of the frame keeps its contrast instead of going grey.\n  if (uScrimDir > 0.5) {\n    float x = uScrimDir < 1.5 ? vUv.x\n            : uScrimDir < 2.5 ? 1.0 - vUv.x\n            : uScrimDir < 3.5 ? 1.0 - vUv.y\n            : vUv.y;\n    c *= 1.0 - uScrimAmt * pow(1.0 - clamp(x, 0.0, 1.0), 2.4);\n  }\n\n  // A grain of dither. Without it these long dark ramps band into rings.\n  float n = fract(sin(dot(gl_FragCoord.xy + uSeed, vec2(12.9898, 78.233))) * 43758.5453);\n  c += (n - 0.5) / 255.0;\n\n  gl_FragColor = vec4(c, 1.0);\n}\n"),!!w&&!!y&&!!E&&!!T&&!!k&&(D=r.createBuffer(),r.bindBuffer(r.ARRAY_BUFFER,D),r.bufferData(r.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),r.STATIC_DRAW),r.enableVertexAttribArray(0),r.vertexAttribPointer(0,2,r.FLOAT,!1,0,0),r.disable(r.DEPTH_TEST),r.disable(r.BLEND),!0)}function z(){for(let e of[S,_,M,A,R])e&&(r.deleteTexture(e.tex),r.deleteFramebuffer(e.fb));S=null,_=null,M=null,A=null,R=null,F=0}function G(){let t=e.getBoundingClientRect(),a=f?1:Math.min(window.devicePixelRatio||1,Math.max(1,N.current.maxDpr)),r=Math.max(1,Math.round(t.width)),o=Math.max(1,Math.round(t.height)),i=f?.34:Math.min(1,Math.max(.4,N.current.resolution)),s=Math.max(2,Math.round(r*a)),l=Math.max(2,Math.round(o*a)),u=Math.max(2,Math.round(s*i)),h=Math.max(2,Math.round(l*i));if(s===C&&l===U&&u===P&&h===L)return;C=s,U=l,P=u,L=h,n.width=s,n.height=l,n.style.width=r+"px",n.style.height=o+"px",z(),S=b(u,h),_=b(u,h),M=b(u,h);let c=Math.max(2,u>>2),d=Math.max(2,h>>2);A=b(c,d),R=b(c,d)}let j=t?6:0,X=0,H=!0,W=!0,q=0;function Y(e,n){r.useProgram(e.program),r.bindFramebuffer(r.FRAMEBUFFER,n?n.fb:null),r.viewport(0,0,n?n.w:C,n?n.h:U)}function V(){r.drawArrays(r.TRIANGLES,0,3)}function K(e,n){r.activeTexture(r.TEXTURE0+n),r.bindTexture(r.TEXTURE_2D,e)}let J=[[.5,.333],[.25,.667],[.75,.111],[.125,.444],[.625,.778],[.375,.222],[.875,.556],[.0625,.889]];function Q(e){if(!w||!y||!E||!T||!k||!S||!_||!M||!A||!R)return;let n=N.current,t=(n.azimuth+n.orbitSpeed*e)*s,a=Math.max(-88,Math.min(88,n.elevation))*s,o=Math.max(2.2,n.distance),i=Math.cos(a),u=o*i*Math.cos(t),h=o*Math.sin(a),c=o*i*Math.sin(t),d=-u/o,p=-h/o,v=-c/o,g=v,b=0,D=-d,P=Math.hypot(g,0,D)||1;g/=P;let L=(b/=P)*v-(D/=P)*p,B=D*d-g*v,I=g*p-b*d,O=Math.cos(n.roll*s),z=Math.sin(n.roll*s),G=g*O+L*z,j=b*O+B*z,X=D*O+I*z,H=-g*z+L*O,W=-b*z+B*O,q=-D*z+I*O,Q=l(n.hotColor),Z=l(n.midColor),$=l(n.coolColor),ee=Math.max(n.diskInner+.5,n.diskOuter);Y(w,S);let en=w.u;r.uniform2f(en.uRes,S.w,S.h),r.uniform1f(en.uTime,e),r.uniform3f(en.uCamPos,u,h,c),r.uniform3f(en.uRight,G,j,X),r.uniform3f(en.uUp,H,W,q),r.uniform3f(en.uFwd,d,p,v),r.uniform1f(en.uTanHalf,Math.tan(.5*Math.max(8,Math.min(110,n.fov))*s)),r.uniform2f(en.uFocus,n.focus[0],1-n.focus[1]),r.uniform1f(en.uSteps,f?130:Math.max(60,Math.min(460,Math.round(n.steps)))),r.uniform1f(en.uSkyR,Math.max(1.35*o,2.4*ee)),r.uniform1f(en.uDiskIn,Math.max(1.05,n.diskInner)),r.uniform1f(en.uDiskOut,ee),r.uniform1f(en.uThick,Math.max(.02,n.diskThickness)),r.uniform1f(en.uDensity,Math.max(0,n.diskDensity)),r.uniform1f(en.uSpin,6.2831853*n.spinSpeed),r.uniform1f(en.uGrain,Math.max(.02,n.grain)),r.uniform1f(en.uBright,Math.max(0,n.brightness)),r.uniform1f(en.uDoppler,Math.max(0,Math.min(1,n.doppler))),r.uniform3f(en.uHot,Q[0],Q[1],Q[2]),r.uniform3f(en.uMid,Z[0],Z[1],Z[2]),r.uniform3f(en.uCool,$[0],$[1],$[2]),r.uniform1f(en.uStars,Math.max(0,n.starBrightness)),r.uniform1f(en.uEncode,m?0:1);let et=J[F%J.length];r.uniform2f(en.uJitter,et[0]-.5,et[1]-.5),r.uniform1f(en.uSeed,F%64*17.13),V();let ea=0===F?1:.14;Y(y,M),K(S.tex,0),K(_.tex,1),r.uniform1i(y.u.uCur,0),r.uniform1i(y.u.uPrev,1),r.uniform1f(y.u.uAlpha,ea),V();let er=M,eo=_;_=M,M=eo,F++,Y(E,A),K(er.tex,0),r.uniform1i(E.u.uTex,0),r.uniform2f(E.u.uTexel,1/er.w,1/er.h),r.uniform1f(E.u.uDecode,m?0:1),r.uniform1f(E.u.uPack,x),r.uniform1f(E.u.uThreshold,.85),V();let ei=(e,n,t,a)=>{Y(T,n),K(e.tex,0),r.uniform1i(T.u.uTex,0),r.uniform2f(T.u.uStep,t/n.w,a/n.h),V()};ei(A,R,1,0),ei(R,A,0,1),ei(A,R,2.6,0),ei(R,A,0,2.6),Y(k,null),K(er.tex,0),K(A.tex,1),r.uniform1i(k.u.uScene,0),r.uniform1i(k.u.uBloom,1),r.uniform2f(k.u.uRes,C,U),r.uniform1f(k.u.uDecode,m?0:1),r.uniform1f(k.u.uPack,x),r.uniform1f(k.u.uGlow,.26*Math.max(0,n.glow)),r.uniform1f(k.u.uExposure,Math.max(.05,n.exposure)),r.uniform1f(k.u.uVignette,Math.max(0,Math.min(1,n.vignette))),r.uniform1f(k.u.uScrimDir,"left"===n.scrim?1:"right"===n.scrim?2:"top"===n.scrim?3:"bottom"===n.scrim?4:0),r.uniform1f(k.u.uScrimAmt,Math.max(0,Math.min(1,n.scrimStrength))),r.uniform1f(k.u.uSeed,60*e%1e3),V()}function Z(e){for(let n=0;n<e;n++)Q(j)}function $(e){if(!H)return;if(q=requestAnimationFrame($),!W){X=e;return}let n=X?Math.min(.05,(e-X)/1e3):0;X=e,N.current.paused||t||(j+=n),Q(j)}if(!O()){o("build-failed");return}G(),Z(t?16:1),t||(q=requestAnimationFrame($));let ee=new ResizeObserver(()=>{G(),(t||N.current.paused)&&Z(16)});ee.observe(e);let en=new IntersectionObserver(e=>{var n,t;W=null===(t=null===(n=e[0])||void 0===n?void 0:n.isIntersecting)||void 0===t||t},{threshold:0});en.observe(e);let et=()=>{W=!document.hidden,X=0},ea=e=>{e.preventDefault(),H=!1,cancelAnimationFrame(q),n.style.display="none"},er=()=>{if(C=U=P=L=0,!O()){o("lost");return}n.style.display="",e.dataset.webgl="",G(),H=!0,X=0,Z(t?16:1),t||(q=requestAnimationFrame($))};return document.addEventListener("visibilitychange",et),n.addEventListener("webglcontextlost",ea),n.addEventListener("webglcontextrestored",er),()=>{for(let e of(H=!1,cancelAnimationFrame(q),ee.disconnect(),en.disconnect(),document.removeEventListener("visibilitychange",et),n.removeEventListener("webglcontextlost",ea),n.removeEventListener("webglcontextrestored",er),z(),D&&r.deleteBuffer(D),[w,y,E,T,k]))e&&r.deleteProgram(e.program)}},[]),(0,a.jsxs)("div",{ref:B,className:"relative isolate h-full w-full overflow-hidden bg-black ".concat(U),...L,children:[(0,a.jsx)("canvas",{ref:I,"aria-hidden":"true",className:"absolute inset-0 h-full w-full"}),P?(0,a.jsx)("div",{className:"relative z-10 h-full w-full",children:P}):null]})}function f(){let e=function(){let e=arguments.length>0&&void 0!==arguments[0]?arguments[0]:"(max-width: 767px)",[n,t]=(0,r.useState)(!1);return(0,r.useEffect)(()=>{let n=window.matchMedia(e),a=()=>t(n.matches);return a(),n.addEventListener("change",a),()=>n.removeEventListener("change",a)},[e]),n}(),n=(0,o.useRouter)(),[t,s]=(0,r.useState)(!1),[l,f]=(0,r.useState)(!1);(0,r.useEffect)(()=>{n.prefetch("/home");let e=window.matchMedia("(prefers-reduced-motion: reduce)");f(e.matches);let t=e=>f(e.matches);return e.addEventListener("change",t),()=>e.removeEventListener("change",t)},[n]);let h=(0,r.useCallback)(e=>{e.preventDefault(),t||(s(!0),setTimeout(()=>{n.push("/home")},l?200:1e3))},[t,l,n]);return(0,a.jsxs)("section",{className:"relative min-h-[92svh] w-full overflow-hidden bg-black md:min-h-[720px]",children:[(0,a.jsx)("div",{className:"absolute inset-0 h-full w-full transition-all ".concat(l?"duration-200":"duration-1000 ease-in-out"," ").concat(t?l?"opacity-0":"scale-[2.4] opacity-0":"scale-100 opacity-100"),style:{transformOrigin:e?"50% 76%":"72% 46%"},children:(0,a.jsx)(u,{focus:e?[.5,.76]:[.72,.46],scrim:e?"top":"left",scrimStrength:.9,distance:24,elevation:e?-7:-5.5,fov:e?58:42,glow:e?.85:1,steps:e?200:300,resolution:e?.6:.7})}),(0,a.jsx)("div",{className:"relative z-10 flex h-full min-h-[100dvh] md:min-h-[720px] items-start px-5 pt-12 sm:px-10 md:items-center md:pt-0 lg:px-20 transition-all ".concat(l?"duration-200":"duration-700 ease-out"," ").concat(t?"opacity-0 blur-md pointer-events-none scale-95":"opacity-100 blur-0 scale-100"),children:(0,a.jsxs)("div",{className:"max-w-[34rem]",children:[(0,a.jsxs)("h1",{className:"text-3xl sm:text-5xl md:text-6xl lg:text-[4.25rem] font-light leading-[1.08] tracking-[-0.03em] text-white",children:["Light does not",(0,a.jsx)("br",{}),"leave here"]}),(0,a.jsx)("p",{className:"mt-4 sm:mt-6 max-w-md text-xs sm:text-[0.95rem] leading-relaxed text-white/60 md:mt-7",children:"The ring above the shadow is the far side of the disc, bent over the top. Nothing put it there but gravity."}),(0,a.jsxs)("div",{className:"mt-6 sm:mt-8 flex flex-wrap items-center gap-2.5 sm:gap-3 md:mt-10",children:[(0,a.jsx)("button",{onClick:h,type:"button",className:"rounded-full bg-[#F5EFEB] px-5 sm:px-6 py-2.5 sm:py-3 text-xs sm:text-sm font-medium text-black transition hover:bg-white focus:outline-none focus:ring-2 focus:ring-[#E58C36] focus:ring-offset-2 focus:ring-offset-black cursor-pointer select-none",children:"Let's start"}),(0,a.jsx)(i.default,{href:"/home#book",className:"rounded-full border border-white/20 px-5 sm:px-6 py-2.5 sm:py-3 text-xs sm:text-sm text-[#F5EFEB]/90 transition hover:border-[#E58C36]/50 hover:text-white focus:outline-none focus:ring-2 focus:ring-[#E58C36] focus:ring-offset-2 focus:ring-offset-black select-none",children:"Book a call"})]})]})})]})}}},function(e){e.O(0,[448,971,117,744],function(){return e(e.s=4400)}),_N_E=e.O()}]);